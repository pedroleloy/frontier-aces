# Base URL for the Express backend. Defaults to "/api" (Vite proxy in dev,
# same-origin in prod). Set to your Render URL when deploying frontend on
# Vercel and backend on Render.
VITE_API_BASE=/api
